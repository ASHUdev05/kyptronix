import React from 'react';

const Hero: React.FC = () => {
  return (
    <section id="hero" className="hero">
      <h1>Read More. Spend Less.</h1>
      <p>Share books, earn rewards, and explore endless learning — all while helping others.</p>
      <button>Join the Community</button>
    </section>
  );
};

export default Hero;
